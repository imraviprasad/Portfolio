import { Box, Typography, styled } from "@mui/material";

namespace S {
  export const FlipCardContainer = styled("div")({
    perspective: "1000px",
    width: "33.3333%",
    height: "200px",
    padding: "10px",
  });
}

export default S;
